name: Publish Base Test Runner Image

on:
  workflow_dispatch:
    inputs:
      image_name:
        description: "GHCR image name"
        required: false
        default: "ghcr.io/ndviet/test-automation-java-common"
      push_image:
        description: "Push built image to GHCR"
        required: true
        type: boolean
        default: true
  push:
    branches:
      - master
    tags:
      - "v*"
    paths:
      - ".github/workflows/publish-base-test-runner.yml"
      - "containers/**"
      - "build.gradle"
      - "settings.gradle"
      - "gradle.properties"
      - "gradle/**"
      - "test-libraries/**"

permissions:
  contents: read
  packages: write

concurrency:
  group: publish-base-test-runner-${{ github.ref }}
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      IMAGE_NAME: ${{ github.event_name == 'workflow_dispatch' && inputs.image_name || vars.GHCR_JAVA_COMMON_IMAGE || 'ghcr.io/ndviet/test-automation-java-common' }}
      JAVA_BASE_IMAGE_NAME: ${{ vars.GHCR_JAVA_BASE_IMAGE || 'ghcr.io/ndviet/test-automation-java-base' }}
    steps:
      - name: Checkout test-automation-fwk
        uses: actions/checkout@v4
        with:
          path: test-automation-fwk

      - name: Set push mode
        id: push_mode
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.push_image }}" = "false" ]; then
            echo "push=false" >> "${GITHUB_OUTPUT}"
          else
            echo "push=true" >> "${GITHUB_OUTPUT}"
          fi

      - name: Resolve image tags
        id: image_meta
        shell: bash
        run: |
          base_revision="$(sed -n 's/^version=//p' test-automation-fwk/gradle.properties | head -n 1)"
          if [ -z "${base_revision}" ]; then
            echo "Could not resolve version from test-automation-fwk/gradle.properties"
            exit 1
          fi

          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            # Tag pushes are release publishes.
            deploy_revision="${base_revision%-SNAPSHOT}"
          else
            # Master (or manual non-tag) publishes snapshot.
            if [[ "${base_revision}" == *-SNAPSHOT ]]; then
              deploy_revision="${base_revision}"
            else
              deploy_revision="${base_revision}-SNAPSHOT"
            fi
          fi
          deploy_tags="${IMAGE_NAME}:latest"
          deploy_tags+=$'\n'"${IMAGE_NAME}:${deploy_revision}"
          java_base_image="${JAVA_BASE_IMAGE_NAME}:${deploy_revision}"

          echo "revision=${deploy_revision}" >> "${GITHUB_OUTPUT}"
          echo "java_base_image=${java_base_image}" >> "${GITHUB_OUTPUT}"
          {
            echo "tags<<EOF"
            echo "${deploy_tags}"
            echo "EOF"
          } >> "${GITHUB_OUTPUT}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: steps.push_mode.outputs.push == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GH_PACKAGES_USERNAME }}
          password: ${{ secrets.GH_PACKAGES_TOKEN }}

      - name: Build and publish base test runner
        uses: docker/build-push-action@v6
        with:
          context: .
          file: test-automation-fwk/containers/base-test-runner/Dockerfile
          push: ${{ steps.push_mode.outputs.push }}
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.image_meta.outputs.tags }}
          build-args: |
            SKIP_FRAMEWORK_TESTS=true
            JAVA_BASE_IMAGE=${{ steps.image_meta.outputs.java_base_image }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Print published tags
        if: steps.push_mode.outputs.push == 'true'
        run: |
          echo "Published image tags:"
          echo "${{ steps.image_meta.outputs.tags }}"
          echo "Built from base image:"
          echo "${{ steps.image_meta.outputs.java_base_image }}"
